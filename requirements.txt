# numpy and scikit-learn are pinned to the exact versions the reported numbers
# were validated on (CPython 3.12.13; see the reproduction certificate).
# matplotlib renders the figures only. CPU only; no GPU.
# Requires CPython 3.11 or newer.
numpy==2.0.2
scikit-learn==1.6.1
matplotlib==3.10.8
